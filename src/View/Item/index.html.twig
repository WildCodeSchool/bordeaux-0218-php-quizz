{% extends 'layout.html.twig' %}
{% block content %}
<h1>Items</h1>
<ul>
  {% for user in users %}
  <li><a href="/user/{{ user.id }}">{{ user.id }}. {{ user.title }}</a></li>
  {% else %}
  <li>Nothing to display</li>
  {% endfor %}
</ul>
{% endblock %}
